<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="cart.css" />
    <title>CoffeeBean</title>
  </head>
  <body>
    <%- include("./header.ejs") %>
    <main>
      <h1>장바구니</h1>

      <form method="post">
        <table>
          <tr>
            <th>상품정보</th>
            <th>수량</th>
            <th>가격</th>
            <th></th>
          </tr>
          <% products.forEach(item => { %>
          <tr>
            <td>
              <%= item.product_name%> <br />
              <%= item.kg %>
            </td>
            <td><%= item.quantity %></td>
            <td><%= item.price %>원</td>
            <td>
              <button type="submit" name="itemID" value="<%= item.cart_id%>">
                <i class="fa-solid fa-xmark"></i>
              </button>
            </td>
          </tr>
          <% }) %>
          <tr>
            <th></th>
            <th>합계</th>
            <td><%= totalPrice %>원</td>
          </tr>
        </table>
      </form>

      <form action="/order" method="post">
        <input type="submit" value="주문하기" />
      </form>
    </main>

    <%- include("./footer.ejs") %>
  </body>
</html>
